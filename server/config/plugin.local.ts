export const rateLimiter = {
    enable: false
};

export const grayscale = {
    enable: false
};

export const trace = {
    enable: false
};

export const disconf = {
    enable: false
};

export const dubbo = {
    enable: false
};

export const health = {
    enable: false
};

export const kagent = {
    enable: false
};